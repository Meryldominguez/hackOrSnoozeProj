
  static async changeUserInfo(userObj){
    const username = localStorage.username;
    const token = localStorage.token
    const submission = await axios.patch(`${BASE_URL}/users/${username}`,{"token": token, "user": {"name":userObj.name, "password": userObj.title, "username": userObj.username}})
    console.log(submission.message)
    
  }



  